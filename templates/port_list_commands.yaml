commands:
  provision:
    create: '/configure application-assurance group {partition} port-list {port_list} create'
    description: '/configure application-assurance group {partition} port-list {port_list} description {description}'
    add_port: '/configure application-assurance group {partition} port-list {port_list} port {port}'
  rollback:
    remove: null
    remove_domain: null
